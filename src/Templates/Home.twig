{% extends 'layout.twig' %}

{# Surcharge du bloc 'content' pour le contenu spécifique à la page d'accueil #}
{% block content %}
    <div class="pt-[32rem] flex flex-col items-center text-xl text-center text-white">
        {# <h1 class="text-6xl font-bold text-white underline text-shadow-black ">{{ title }}</h1> #}
        <h1 class="text-6xl font-bold text-white text-shadow-black ">Bienvenue à ARCADIA</h1>
        <p class="relative z-0 w-2/4 pt-8 top-2/4">{{ message }}</p>

        {# Div pour l'injection dynamique des données par app.js #}
        <div id="cards"></div>
    </div>
    <div class="w-full bg-slate-700 h-44"><p class="text-center text-white text">test</p></div>
{% endblock %}
